[project]
name = "bhoomi-setu-rules"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["pyyaml>=6.0,<7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bhoomi_setu_rules"]

