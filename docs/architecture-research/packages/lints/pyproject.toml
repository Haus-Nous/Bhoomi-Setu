[project]
name = "bhoomi-setu-lints"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
bhoomi-lint = "bhoomi_setu_lints.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bhoomi_setu_lints"]
